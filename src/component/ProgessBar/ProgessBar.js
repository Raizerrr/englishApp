import { useState } from "react";

function ProgessBar() {
  const [filles, setFilled] = useState(0);
  const [running, isRunning] = useState(true);
  return <div></div>;
}

export default ProgessBar;
